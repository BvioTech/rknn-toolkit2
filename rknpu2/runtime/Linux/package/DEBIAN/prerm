#!/bin/sh
set -e

if [ "$1" = "remove" ] || [ "$1" = "upgrade" ] || [ "$1" = "deconfigure" ]; then
    if command -v systemctl >/dev/null 2>&1; then
        systemctl stop rknn-server.service || true
        systemctl disable rknn-server.service || true
    fi
fi

exit 0
